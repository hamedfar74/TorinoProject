import AuthModal from "../modules/AuthModal"

const Homepage = () => {
  // throw new Error("خطایی رخ داده")
  return (
    <div>
    <h1>Homepage</h1>
    
    <AuthModal />
    </div>
  )
}

export default Homepage